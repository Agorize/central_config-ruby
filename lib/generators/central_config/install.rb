# frozen_string_literal: true

CentralConfig.configure do |config|
  # config.adapter = CentralConfig::Adapters::Flagr.new
  # config.error_handler = ->(exception) { … }
  # config.flagr_host = ENV['CENTRAL_CONFIG_FLAGR_HOST']
end
